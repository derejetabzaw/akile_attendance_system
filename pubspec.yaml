name: akile_attendance_system
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0
  provider: ^4.2.0
  shared_preferences: ^0.5.2
  http: ^0.12.0+4
  rounded_letter: ^0.0.6
  cached_network_image: 2.0.0-rc.1
  device_id: ^0.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
  assets:
    - logo.jpg
    - AKILLE-01.jpg

