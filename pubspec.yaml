name: akile_attendance_system
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
#  parse_server_sdk_flutter: ^3.1.0
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.0
  provider: ^4.2.0
  shared_preferences: ^0.5.2
  http: ^0.12.0+4
  rounded_letter: ^0.0.6
  cached_network_image: 2.0.0-rc.1
  device_id: ^0.2.0
  flutter_launcher_icons: ^0.8.1
  geolocator: ^6.1.13
  mongo_dart: ^0.4.4
dev_dependencies:
#  build_runner:
#    json_serialization:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
  assets:
    - logo.jpg
    - AKILLE-01.jpg

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/AKILLE-01.jpg"


